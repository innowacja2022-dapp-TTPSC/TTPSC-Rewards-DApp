import { Button, Flex, Text } from "@chakra-ui/react";
import { ReactElement } from "react";

export const HireEmployee = (): ReactElement => {
  return (
    <Flex>
      <Text>Hire Employee</Text>
      <Button>Hire</Button>
    </Flex>
  );
};
